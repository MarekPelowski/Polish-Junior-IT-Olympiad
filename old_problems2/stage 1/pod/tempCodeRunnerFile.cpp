        // for (int d = 0; d <= 9; d++) {
        //     std::cout << d << std::endl;
        // //     if (i % pr[d + 1] >= pr[d]) {
        // //             for (int mod = 0; mod < k; mod++) {
        // //             int new_mod = (10 * mod + d) % k;
        // //             DP[i - pr[d]][new_mod] += DP[i][mod];
        // //         }
        // //     }
        // }