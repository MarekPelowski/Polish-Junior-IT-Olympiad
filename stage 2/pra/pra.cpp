#include "bits/stdc++.h"

using namespace std;

int main()
{
    int N;
    cin >> N;
    vector<pair<int, int>> work_hours(N);

    for(int i = 0; i < N; i++){
        cin >> work_hours[i].first >> work_hours[i].second;
    }

    

    return 0;
}